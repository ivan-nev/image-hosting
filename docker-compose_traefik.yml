services:
  db:
    image: postgres:15
    container_name: db_image
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - proxy-network

  app:
    build: .
    container_name: app_image
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - images_data:/images
      - logs_data:/logs
    restart: unless-stopped
    networks:
      - proxy-network
    labels:
      - "traefik.enable=true"
      # HTTP to HTTPS redirect
      - "traefik.http.routers.app-http.entrypoints=web"
      - "traefik.http.routers.app-http.rule=Host(`${DOMAIN}`)"  # ← БЕЗ подчеркивания
      - "traefik.http.routers.app-http.middlewares=redirect-to-https@file"
      # HTTPS с SSL
      - "traefik.http.routers.app.entrypoints=websecure"
      - "traefik.http.routers.app.rule=Host(`${DOMAIN}`)"  # ← БЕЗ подчеркивания
      - "traefik.http.routers.app.tls.certresolver=le"
      - "traefik.http.services.app.loadbalancer.server.port=8000"


volumes:
  db_data:
  images_data:
  logs_data:

networks:
  proxy-network:
    external: true
    name: traefik_proxy-network